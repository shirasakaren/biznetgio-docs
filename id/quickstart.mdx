---
title: "Quickstart (Terraform)"
description: "Order VM NEO Lite pake provider Terraform Biznet GIO"
---

Dari nol sampe VM NEO Lite jalan, kurleb lima menit.

## Prasyarat

- [Terraform](https://developer.hashicorp.com/terraform/install) ≥ 1.0
- Akun [Biznet GIO](https://biznetgio.com)
- API token - liat [Authentication](/id/authentication)

## Gas

<Steps>
  <Step title="Export API token lu">
    Provider baca credentials dari environment variables, jadi gak butuh provider block:

    ```bash
    export BIZNETGIO_API_KEY="<your-token>"
    ```
  </Step>

  <Step title="Tulis konfigurasinya">
    Bikin `main.tf`:

    ```hcl
    terraform {
      required_providers {
        biznetgio = {
          source = "registry.terraform.io/shirasakaren/biznetgio"
        }
      }
    }

    provider "biznetgio" {}

    data "biznetgio_neolite_products" "all" {}

    data "biznetgio_neolite_os_list" "ubuntu" {
      product_id = data.biznetgio_neolite_products.all.products[0].product_id
    }

    resource "biznetgio_neolite_keypair" "main" {
      name = "neo-lite-key"
    }

    resource "biznetgio_neolite_vm" "main" {
      ssh_and_console_user = "adminuser"
      console_password     = "s3cretP4ssw0rd"
      vm_name              = "neo-lite-1"
      product_id           = data.biznetgio_neolite_products.all.products[0].product_id
      select_os            = data.biznetgio_neolite_os_list.ubuntu.oss[0].name
      keypair_id           = biznetgio_neolite_keypair.main.keypair_id
      cycle                = "m"
    }

    output "vm_status" {
      value = biznetgio_neolite_vm.main.status
    }
    ```

    `products[0]` dan `oss[0]` itu ngambil produk dan OS apapun yang kebetulan dibalikin API duluan - **bukan berarti yang paling murah atau paling kecil.** Quickstart ini pake itu biar bisa langsung jalan sekali copy-paste, tapi sebelum lu biarin gini di akun yang lu peduliin cost-nya, baca [Paham katalog produk](/id/products/overview) dan ganti `[0]` dengan filter buat paket (misalnya tier NEO Lite paling murah, `XS 1.1`) dan OS yang lu beneran mau - liat [katalog NEO Lite](/id/products/neolite) buat tabel harga lengkap dan filter copy-paste-nya.

    <Warning>
      Step ini order beneran di harga apapun yang kebetulan jadi produk pertama di list. Kalau lu mau mastiin harga paling murah sebelum jalanin `terraform apply`, lakuin filter di [Contoh nyata: milih XS 1.1](/id/products/neolite#contoh-nyata-milih-xs-1-1-buat-harga-paling-murah) dulu.
    </Warning>
  </Step>

  <Step title="Apply">
    ```bash
    terraform init
    terraform plan
    terraform apply
    ```

    Konfirmasi plan dengan `yes`. Bikin VM artinya order beneran di akun lu dan biasanya makan 1-5 menit.
  </Step>

  <Step title="Verifikasi">
    ```bash
    terraform output vm_status
    ```

    Harusnya keliatan `"Active"`. VM lu juga keliatan di bagian NEO Lite di [portal](https://portal.biznetgio.com).
  </Step>

  <Step title="Beresin">
    ```bash
    terraform destroy
    ```

    Destroy resource VM berarti hapus server (`DELETE /neolites/{account_id}`).
  </Step>
</Steps>

## Lanjut ke mana

- [Katalog Produk](/id/products/neolite) - harga NEO Lite beneran dan cara milih paket tertentu, bukan asal `[0]`
- [Instalasi Terraform](/id/terraform/installation) - referensi konfigurasi provider lengkap
- [Resource NEO Lite](/id/terraform/resources/neolite) - semua resource dan data source NEO Lite
- [Billing](/id/guides/billing) - gimana order dan pembayaran kerja

<Tip>
  Nyangkut? Hubungi support Biznet GIO di [support@biznetgio.com](mailto:support@biznetgio.com), atau buka issue di [repository provider](https://github.com/shirasakaren/terraform-provider-biznetgio).
</Tip>
